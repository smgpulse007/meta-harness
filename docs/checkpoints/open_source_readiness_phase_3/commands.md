# Commands

Commands were run from `C:\Users\shail\OneDrive\Documents\meta-harness` on branch `open-source-readiness-phase-3`.

| Command                                                                                                         | Exit | Evidence                                                                                                                               |
| --------------------------------------------------------------------------------------------------------------- | ---: | -------------------------------------------------------------------------------------------------------------------------------------- |
| `pnpm install --frozen-lockfile`                                                                                |    0 | Already up to date with pnpm 11.7.0.                                                                                                   |
| `pnpm run clean`                                                                                                |    0 | Cleaned all package `dist` and `.tsbuildinfo` outputs.                                                                                 |
| `pnpm --filter @meta-harness/cli test` before package pretest recovery                                          |    1 | After `pnpm run clean`, Vitest could not resolve `@meta-harness/core` because workspace package `dist` outputs were absent.            |
| `pnpm --filter @meta-harness/cli test` after package pretest recovery                                           |    0 | Dependency closure built first; 3 passed, 1 skipped.                                                                                   |
| `pnpm --filter @meta-harness/cli build`                                                                         |    0 | TypeScript build passed after emitter and context-pack changes.                                                                        |
| `node packages/cli/dist/index.js emit-instructions --target all`                                                |    0 | Wrote 12 instruction/config files.                                                                                                     |
| `node packages/cli/dist/index.js emit-instructions --help`                                                      |    0 | Help listed `agents`, `claude`, `gemini`, `cursor`, `windsurf`, `copilot`, `copilot-custom`, `continue`, `opencode`, `roo`, and `all`. |
| `node packages/cli/dist/index.js context-pack --target codex --phase phase_001 --budget 8000 --format markdown` |    0 | Emitted a bounded Codex Markdown context pack with status `within_budget`.                                                             |
| `node packages/cli/dist/index.js prompt --target roo --format json`                                             |    0 | JSON parsed; target `roo`; status `within_budget`; 10 files in pack.                                                                   |
| `node packages/cli/dist/index.js --help`                                                                        |    0 | Help listed `context-pack`, `prompt`, and `emit-instructions`.                                                                         |
| `node packages/cli/dist/index.js context-pack --target codex --phase phase_001 --budget 8000 --format json`     |    0 | JSON parsed; target `codex`; status `within_budget`; `AGENTS.md` exists with root instruction excerpt.                                 |
| Filtered MCP SDK smoke using `process.cwd()` as transport cwd                                                   |    1 | Recovery finding: `pnpm --filter ... exec` changed cwd to `packages/mcp-server`, so root-relative CLI command resolution failed.       |
| `node packages/cli/dist/index.js mcp --stdio` via MCP SDK client with explicit repo-root transport cwd          |    0 | `get_harness_status` returned root harness state with `current_phase_id: phase_7`.                                                     |
| JSON parse check for root and docs MCP config samples                                                           |    0 | Parsed 9 JSON files including `opencode.json` and `.roo/mcp.json`.                                                                     |
| YAML parse check for `docs/examples/mcp-configs/continue-mcpServers.yaml`                                       |    0 | Parsed successfully with `yaml`.                                                                                                       |
| `pnpm run ci`                                                                                                   |    0 | Lint, typecheck/build, 17 tests, schema check, build, and integration smoke passed.                                                    |
| `pnpm docs:build`                                                                                               |    0 | TypeDoc generated Markdown and VitePress build completed.                                                                              |
| `pnpm audit --prod`                                                                                             |    0 | No known vulnerabilities found.                                                                                                        |
| `pnpm --filter @meta-harness/core pack --dry-run`                                                               |    0 | Dry-run tarball contained expected core files.                                                                                         |
| `pnpm --filter @meta-harness/adapters pack --dry-run`                                                           |    0 | Dry-run tarball contained expected adapter files.                                                                                      |
| `pnpm --filter @meta-harness/cli pack --dry-run`                                                                |    0 | Dry-run tarball included `dist/commands/context-pack.*`.                                                                               |
| `pnpm --filter @meta-harness/mcp-server pack --dry-run`                                                         |    0 | Dry-run tarball contained expected MCP server files.                                                                                   |
| `git diff --check`                                                                                              |    0 | No whitespace errors.                                                                                                                  |
| Targeted `pnpm exec prettier --check --ignore-unknown <Phase 3 touched files>`                                  |    0 | All matched Phase 3 touched files used Prettier style.                                                                                 |
| `pnpm format:check`                                                                                             |    1 | Non-blocking: repo-wide check failed on broad pre-existing formatting debt. Phase 3 touched files passed scoped Prettier.              |
| Final reviewer `019f3afa-5808-7520-b7c8-58c3a9b2f6b2`                                                           |    0 | Returned `aligned`, `on_track`, `strong`, 97 percent phase completion, no blockers, and `continue`.                                    |

Generated `.tgz` dry-run artifacts were removed after verification.
