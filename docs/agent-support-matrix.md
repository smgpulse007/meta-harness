# Agent Support Matrix

Last refreshed: 2026-07-07.

This matrix tracks agent support by integration surface. It is intentionally not a single "supported" boolean: Meta Harness can support instruction files, MCP configuration, skills, or prompt-file fallback without supporting native process dispatch.

Proof status values used here are `static_verified`, `command_verified`, `partial`, `claimed`, and `not_verified`. Roadmap intent is described in target/support/gap text, not as a proof status.

## Support Tiers

| Tier | Meaning                                                                                             |
| ---: | --------------------------------------------------------------------------------------------------- |
|    0 | Complete local harness surface. Implemented and exercised locally.                                  |
|    1 | Instruction or rule surface only. No native launch is claimed.                                      |
|    2 | Conservative adapter detection plus filesystem prompt fallback. Native launch is disabled.          |
|    3 | Experimental native dispatch target. Must remain feature-gated until local command evidence exists. |

## Current Matrix

| Target                           | Tier | Instruction Surface                                                                           | Skill Or Plugin Surface                                                              | MCP Surface                                                                                     | Native Or Headless Dispatch                                                                  | Current Meta Harness Support                                                                              | Proof Status       | Gaps                                                                                                                                   |
| -------------------------------- | ---: | --------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------- | ------------------ | -------------------------------------------------------------------------------------------------------------------------------------- |
| filesystem                       |    0 | Prompt file contract                                                                          | Not applicable                                                                       | Not applicable                                                                                  | No external process launch                                                                   | Complete prompt-file adapter writes worker prompts and collects packets                                   | `command_verified` | Universal fallback only; does not run agents                                                                                           |
| fake                             |    0 | Prompt file contract                                                                          | Not applicable                                                                       | Not applicable                                                                                  | Simulated packet generation only                                                             | Complete deterministic test adapter for examples and smoke tests                                          | `command_verified` | Simulation evidence is not implementation proof                                                                                        |
| Codex                            |    2 | `AGENTS.md` and project `.codex` guidance are documented Codex surfaces                       | Agent Skills and plugins are documented Codex surfaces                               | Codex supports MCP configuration; `codex mcp-server` exposes Codex as an MCP server             | `codex exec` is documented, but Meta Harness does not launch it                              | Adapter detects `codex`, advertises MCP/write-scope hints, and falls back to filesystem prompts           | `partial`          | Verify `codex exec --json`, packet schema enforcement, token usage capture, sandbox and approval flags before enabling native dispatch |
| Claude Code                      |    2 | `CLAUDE.md` and `.claude` project configuration are documented surfaces                       | `.claude/skills/meta-harness/SKILL.md` is provided; plugins remain planned           | `.mcp.json` / MCP config are documented; sample config is provided                              | `claude -p` with JSON and schema output is documented, but Meta Harness does not launch it   | Adapter detects `claude`, emits instructions/skill sample, and falls back to filesystem prompts           | `partial`          | Add hooks guidance and native dispatcher only after local command evidence                                                             |
| Cursor                           |    1 | `.cursor/rules/*.mdc` and `AGENTS.md` are documented                                          | `.agents/skills/meta-harness/SKILL.md` is provided for compatible skill hosts        | `.cursor/mcp.json` with stdio, SSE, and Streamable HTTP is documented; sample exists            | Cursor `agent -p` headless mode is documented, but Meta Harness does not launch it           | Emits `.cursor/rules/meta-harness.mdc` with frontmatter and provides MCP sample                           | `partial`          | Add ignore-file guidance and native dispatch only after local verification                                                             |
| Gemini CLI                       |    2 | `GEMINI.md` hierarchical and just-in-time context files are documented                        | No Meta Harness Gemini skill package yet                                             | `mcpServers` supports stdio, SSE, and Streamable HTTP; settings sample exists                   | `gemini --prompt` / `-p` and JSON output are documented, but Meta Harness does not launch it | Emits `GEMINI.md`; adapter detects `gemini` and falls back to prompts                                     | `partial`          | Verify approval/sandbox behavior before native dispatch                                                                                |
| GitHub Copilot                   |    1 | `.github/copilot-instructions.md` and `.github/instructions/*.instructions.md` are documented | Copilot cloud agent skills/custom agents are documented outside current repo support | MCP is supported across Copilot CLI, app, cloud agent, and code review contexts; sample exists  | Treat cloud agent as task/PR workflow, not local dispatcher                                  | Emits repository and path-specific instruction files with `applyTo`; adapter has no local process control | `partial`          | Add cloud-agent task prompt samples; keep repository MCP allowlisted because Copilot uses tools autonomously                           |
| Windsurf / Devin Desktop Cascade |    1 | `AGENTS.md`, `.devin/rules`, and legacy `.windsurf/rules` are documented                      | Cascade skills are documented with progressive disclosure                            | `mcp_config.json` supports stdio, HTTP, and SSE with tool toggles/admin controls; sample exists | No Meta Harness native launch                                                                | Emits legacy `.windsurf/rules/meta-harness.md`; adapter detects `windsurf` and falls back to prompts      | `partial`          | Add preferred `.devin` guidance and skill/workflow packaging only after format verification                                            |
| Continue                         |    1 | `.continue/rules/*.md` rules are documented                                                   | No current Meta Harness skill surface                                                | `.continue/mcpServers` supports YAML and imported JSON MCP configs; sample exists               | No native dispatch claim                                                                     | Emits `.continue/rules/meta-harness.md`                                                                   | `partial`          | Keep as compatibility target rather than fast-moving native adapter                                                                    |
| Aider                            |    2 | Prompt-file fallback only                                                                     | No skill/plugin support claimed                                                      | No Meta Harness MCP support claimed                                                             | Aider scripting via `--message` is documented, but Meta Harness does not launch it           | Adapter detects `aider` and falls back to prompts                                                         | `partial`          | Verify noninteractive behavior, dry-run, commits, and packet handoff before dispatch claims                                            |
| OpenCode                         |    2 | `AGENTS.md`, `CLAUDE.md` fallback, and `opencode.json` instruction globs are documented       | Agent Skills are documented                                                          | OpenCode config includes MCP server support; local config is emitted                            | No Meta Harness native launch                                                                | Adapter detects `opencode`; `mh emit-instructions --target opencode` emits instructions and MCP config    | `partial`          | Verify native OpenCode launch only after local command evidence                                                                        |
| Roo Code / Cline-style agents    |    1 | `.roo` rules and `.roorules` custom instruction files are documented                          | No Meta Harness skill support claimed                                                | `.roo/mcp.json` project MCP config is documented and emitted                                    | No native dispatch claim                                                                     | `mh emit-instructions --target roo` emits `.roo/rules/meta-harness.md` and `.roo/mcp.json`                | `partial`          | Treat as instruction/MCP compatibility only until local verification                                                                   |
| generic                          |    0 | Uses filesystem prompt contract                                                               | Not applicable                                                                       | Optional through host-specific docs                                                             | No external process launch                                                                   | Covered by filesystem adapter                                                                             | `command_verified` | Use when no host-specific integration is safe or verified                                                                              |

## Local Evidence Summary

- `pnpm run ci` exited 0 on 2026-07-07 before this matrix update.
- `packages/adapters/src/index.ts` registers `filesystem`, `fake`, `codex`, `claude-code`, `cursor`, `gemini`, `copilot`, `windsurf`, `aider`, and `opencode`.
- `packages/adapters/src/cli-adapter.ts` keeps automatic process launch disabled for configurable CLI adapters and returns users to prompt files.
- `packages/cli/src/commands/emit-instructions.ts` emits `AGENTS`, `CLAUDE`, `GEMINI`, `CURSOR`, `WINDSURF`, `COPILOT`, `COPILOT_CUSTOM`, `CONTINUE`, `OPENCODE`, and `ROO`; `all` emits 12 files because OpenCode and Roo include config files.
- `packages/cli/src/commands/context-pack.ts` emits bounded Markdown or JSON target-specific context packs for Codex, Claude Code, Cursor, Gemini, Copilot, Windsurf, Continue, Aider, OpenCode, Roo, and generic filesystem flows.
- `.github/instructions/meta-harness.instructions.md` includes `applyTo: "**"` frontmatter.
- OpenCode and Roo compatibility configs exist at `opencode.json`, `.opencode/instructions/meta-harness.md`, `.roo/rules/meta-harness.md`, and `.roo/mcp.json`.
- MCP sample configs exist under `docs/examples/mcp-configs/`.

## Source Notes

The source-backed research note for this matrix is `docs/research/agent-compatibility-2026-07-07.md`. The local claim ledger is `docs/research/agent-support-claim-ledger-2026-07-07.md`.
