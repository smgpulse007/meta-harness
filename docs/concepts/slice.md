# Slice

A slice is a bounded implementation contract. It includes objective, dependencies, spec references, allowed and forbidden write scopes, validation commands, evidence requirements, packet requirements, fallback policy, and downstream dependencies.

Slices are not generic tasks; they are evidence-producing contracts.
