# Phase

A phase is a bounded stage in a larger implementation spec. It names source spec references, input checkpoints, output checkpoint path, acceptance criteria, validation commands, terminal statuses, human acceptance policy, side-effect policy, and slice DAG.

Phases cannot advance when required gates fail.
