# Packet

A packet is the evidence returned by a worker agent.

It includes owner data, changed files, spec alignment, implementation summary, proof statements, validation command outputs, side-effect safety status, blockers, risks, delegation metrics, interruptions, takeovers, and recovery events.
