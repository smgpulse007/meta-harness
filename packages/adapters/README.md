# @meta-harness/adapters

Agent adapter interfaces and starter adapters for Meta Harness.

## Package Contents

The npm package is intentionally limited to compiled `dist` output, this README, LICENSE, and package metadata. Source files, tests, and checkpoint artifacts stay in the repository.

Publishing requires explicit maintainer approval outside the dry-run workflow.
