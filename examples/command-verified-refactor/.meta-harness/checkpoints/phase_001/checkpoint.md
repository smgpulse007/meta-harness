# Command-Verified Example Checkpoint

Status: `complete`

The example phase added a tiny calculator function and proved it with `node --test tests/math.node-test.mjs`. Unlike fake-adapter examples, the packet uses `evidence_kind: command` and the required proof claim is `command_verified`.
